// Dominio público del sitio, usado para metadata (OG, JSON-LD, sitemap).
// IMPORTANTE: placeholder hasta que el cliente confirme el dominio final de
// producción — actualizar NEXT_PUBLIC_SITE_URL en Vercel cuando se defina.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://www.drjavierpaino.com";
